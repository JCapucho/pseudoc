// This is a test file for pseudoc
entry soma {
    let a: int; 
    Out("Inserir A");
    In(a);
    let b: int;
    Out("Inserir B");
    In(b);
    let soma = a + b;
    Out("A soma de a + b é ", soma);
}
